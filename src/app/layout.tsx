import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Abhinay Kumar Sahu | Portfolio",
  description: "BCA Student | Aspiring App Developer | Passionate about Android Development, AI/ML, and Mathematics",
  keywords: ["portfolio", "app developer", "Android development", "AI/ML", "BCA student", "Abhinay Kumar Sahu"],
  authors: [{ name: "Abhinay Kumar Sahu" }],
  creator: "Abhinay Kumar Sahu",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
